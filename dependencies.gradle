ext {
    versions = [
            versionCode      : VERSION,
            versionName      : VERSION_NAME,
            compileSdkVersion: 28,
            minSdkVersion    : 14,
            targetSdkVersion : 28,
            junit            : '4.12',
            rules            : '1.0.2',
            runner           : '1.0.2',
            uiautomator      : '2.1.3',
            appcompat        : '28.0.0'
    ]

    bintrayUser = BINTRAY_USERNAME
    bintrayKey = BINTRAY_API_KEY
}