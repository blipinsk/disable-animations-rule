apply plugin: 'com.android.library'

android {
    compileSdkVersion versions.compileSdkVersion
    defaultConfig {
        minSdkVersion versions.minSdkVersion
        targetSdkVersion versions.targetSdkVersion
        versionCode versions.versionCode.toInteger()
        versionName versions.versionName
    }
}

dependencies {
    compileOnly "junit:junit:${versions.junit}"
    compileOnly "com.android.support.test:runner:${versions.runner}"
    implementation "com.android.support.test.uiautomator:uiautomator-v18:${versions.uiautomator}"
}

apply from: rootProject.file('gradle/upload_to_bintray.gradle')
